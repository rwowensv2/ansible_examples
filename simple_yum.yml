---
- name: patch rhel
  hosts: centos67.owens.org
  become: yes
  become_method: sudo
  become_user: root

  tasks:
  - name: patch boxes
    yum: 
      name: '*'
      state: latest
      #       exclude: kernel*
