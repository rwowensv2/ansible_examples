---
- name: patch rhel
  hosts: centos
  become: yes
  become_method: sudo
  become_user: root
  roles:
    - common

  tasks:
  - name: patch boxes
    yum: 
      name: '*'
      state: latest
      #       exclude: kernel*
